[code:javascript name:${endpoint}-request]
const response = await fetch('${base-url}/${endpoint}', {
  method: '${method}',
  headers: {
    'Authorization': 'Bearer ${api-key}',
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
console.log(data);
[/code:javascript]